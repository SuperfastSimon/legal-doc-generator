services:
  - type: web
    name: legal-doc-generator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: BASE_URL
        value: https://legal-doc-generator.onrender.com
